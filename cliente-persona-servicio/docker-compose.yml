services:
  db:
    image: postgres:14.4
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bdd_clientes_persona
    container_name: clientes_persona
    volumes:
      - ./postgres:/var/lib/postgresql/data